import React from "react";
import PureCarousel from "../components/PureCarousel";

type Props = {};

const page = (props: Props) => {
  return (
    <div className=" ">
      <PureCarousel />
    </div>
  );
};

export default page;
